# ============================================================================
# 运行时（必须）
# ============================================================================
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.7,<3
# ★ 必需：/api/verify 是 multipart 上传接口，缺了它 FastAPI 会在运行时直接 500。
#   （很容易漏 —— 本地能跑的老接口没有文件上传，装了没装都看不出来）
python-multipart>=0.0.9
google-genai>=1.0
httpx>=0.27

# ============================================================================
# 云端落库（可选 —— 刻意做成"可装可不装"）
# 装了走 SDK；没装时 app/db/rest.py 会用 httpx 直连 PostgREST，
# 功能等价（表/RPC/视图都能用）。本仓库的开发机就从未装过它。
# ============================================================================
supabase>=2.6,<3

# ============================================================================
# 附件读取（可选 —— 缺失时 readers.py 有 stdlib 兜底，已验证 100% 覆盖官方附件）
# 装上更稳（真 PDF 解析/坐标/表格），建议生产前安装。
# ============================================================================
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1

# ============================================================================
# 开发（可选）
# ============================================================================
pytest>=8.0
ruff>=0.5
